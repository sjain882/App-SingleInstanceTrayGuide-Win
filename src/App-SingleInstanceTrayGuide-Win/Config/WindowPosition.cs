namespace App_SingleInstanceTrayGuide_Win.Config
{
    /// <summary>
    /// Represents the possible positions for a window on screen.
    /// Used by WindowPositionConfig to calculate the correct coordinates.
    /// </summary>
    public enum WindowPosition
    {
        TopLeft,
        TopRight,
        BottomLeft,
        BottomRight
    }
}